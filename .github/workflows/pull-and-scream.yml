name: Pull and Scream

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: "self-hosted"

        steps:
            - name: "Cd intranet"
              run: "cd c:/source/Consulting/intranet"
            - name: Status
              run: "git status"