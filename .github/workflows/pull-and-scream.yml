name: Pull and Scream

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: "self-hosted"

        steps:
            - name: "Cd intranet"
              run: "cd c:/source/Consulting/intranet"
            - name: Pull
              run: "git pull"
            - name: Status
              run: "git status"